/* =====================================================================
   MAIN  –  application entry point
   Runs after all other scripts have loaded.
   ===================================================================== */

loadStorage();
renderCityMarkers();
updateStats();
renderPanel();
